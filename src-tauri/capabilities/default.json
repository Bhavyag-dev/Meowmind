{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the companion and settings windows",
  "windows": ["companion", "settings"],
  "permissions": [
    "core:default",
    "core:window:default",
    "opener:default"
  ]
}
