# Rule: No Bulky Comments or Divider Banners

- Never use decorative ASCII divider banners (e.g. `// -------------------`, `/* ===== */`, `// *****`).
- Use concise, informative single-line comments (`// ...`) or standard doc comments (`/** ... */`, `/// ...`).
- Use standard spacing (blank lines) to separate logical blocks of code.
